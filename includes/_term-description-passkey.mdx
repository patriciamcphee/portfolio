A Passkey is a wrapper on top of an X.509 certificate. A passkey contains a public/private key pair where the private key is store securely in a Trust Execution Environment (TEE), or Secure Enclave on macOS and iOS. 